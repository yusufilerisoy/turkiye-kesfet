/* router.jsx — Ekran enum'u (state-based router için) */

const SCREENS = {
  WELCOME: 'welcome',
  MAP: 'map',
  REGION_INTRO: 'region_intro',
  MISSION: 'mission',
  BADGE: 'badge',
  QUIZ: 'quiz',
  FINAL: 'final',
};

Object.assign(window, { SCREENS });
